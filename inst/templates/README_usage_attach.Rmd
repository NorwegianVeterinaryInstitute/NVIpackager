The `r NVIpkg_inline` package needs to be attached.

``` {r attachtext, message=FALSE, warnings=FALSE, echo=FALSE, prompt = FALSE, comment = ""} 
attachtext <- paste0('library(', NVIpkg, ')')
cat(attachtext, sep="", fill = TRUE)
```
